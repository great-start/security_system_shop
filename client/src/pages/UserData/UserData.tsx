import React, { FC } from 'react';
import { Container } from 'react-bootstrap';

export const UserData: FC = () => {
    return (
        <Container>
          UserData
        </Container>
    );
};
